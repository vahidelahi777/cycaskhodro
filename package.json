{
  "name": "cycaskhodro",
  "private": true,
  "version": "1.0.0",
  "packageManager": "npm@11.14.1",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev --filter=frontend",
    "build": "turbo run build --filter=frontend",
    "lint": "turbo run lint --filter=frontend",
    "type-check": "turbo run type-check --filter=frontend"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
